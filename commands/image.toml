name = "/vibe:image"
description = "Render the VibeGraphic static infographic from a spec JSON using nano banana."
prompt = """
You are wired into the VibeGraphics MCP server.

Flow:

1) Ask the user for:
   - spec_path (JSON produced by `vibe_plan_infographic`).

2) Call `vibe_render_image_from_spec` with:
   - spec_path = what they provide
   - out_dir = a reasonable directory (current working directory or one the user requests)
   - model = "gemini-3-flash-image-preview"
   - n = 1 unless the user asks for multiple variations

3) Show the user:
   - The generated image path(s).
   - Confirm that the image was generated using the theme/style encoded in the VibeGraphic spec
     (e.g., 60s space race, blueprint, cyberpunk neon, minimalist diagram — whatever the spec defines).

If the spec is missing `imagePrompt`, explain the issue and suggest re-running `/vibe:plan`
to regenerate a proper VibeGraphic spec.
"""
